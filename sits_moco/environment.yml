# DEPRECATED — use pyproject.toml + uv instead:
#
#   uv sync
#   cp .env.example .env   # set SITS_MOCO_DATAPATH
#
# This file is kept only as a pointer so old conda docs do not look current.
name: sits-moco-deprecated
channels:
  - conda-forge
dependencies:
  - python>=3.10,<3.13
  - pip
  - pip:
      - -e .
